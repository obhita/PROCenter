@model bool

@{
    var ariaLabel = ViewData["section"] + " " + ViewData["group"] + " " + ViewData["labelString"];
}

@Html.CheckBoxFor( m => m, new {id = ViewData["id"], aria_label = @ariaLabel } )